# Local dev database (Prisma CLI uses this for db push / generate).
# The libsql adapter resolves file: paths relative to CWD, so use
# prisma/dev.db to match where Prisma's binary engine puts the file.
DATABASE_URL="file:prisma/dev.db"
